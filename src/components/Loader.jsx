import React from "react";

export default function Loader(){
    return (
        <div id="clockLoader" className="clock-wrapper clock-hidden">
            <div className="clock-loader"></div>
        </div>
    );
}